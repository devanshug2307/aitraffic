# Reserved for the future standalone/BYO Google OAuth connector.
# The current external-profile adapter does not read these variables or import tokens.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# MCP log analysis is restricted to the current project by default.
# Set this only in a trusted local environment.
AITRAFFIC_ALLOW_OUTSIDE_PROJECT=0
